<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Jobservation - Edit/Remove Application</title>
</head>
<body>
  <nav>
    <ul id="nav-bar">
      <li>
        <a href="/organizer" class="nav-bar-link">Home</a>
      </li>

      <li>
          <a href="/logout" class="nav-bar-link">Logout</a>
      </li>
    </ul>
  </nav>
  
  <% if @applications != [] %>
  <header>
      <h1>Please select a application to edit or remove:</h1>
  </header>

  <section class="application-selection">
    <table>
      <tr>
        <th></th>
        <th>Company</th>
        <th>Position</th>
        <th>Date</th>
        <th>Status</th>
      </tr>

      <% @applications.each do |a| %>
        <tr>
            <td>
              <a href="/organizer/edit-remove-application/<%= a.id %>">Edit</a>
              
              <form action="/organizer/edit-remove-application/<%= a.id %>" method="post">
                  <input id="hidden" type="hidden" name="_method" value="delete">
                  <input type="submit" value="Remove">
              </form>
            </td>

            <td><%= a.company %></td>
            <td><%= a.position %></td>
            <td><%= a.date %></td>
            <td><%= a.status %></td>
        </tr>
      <% end %>
    </table>
  </section>
  <% else %>
    <h1 id="no-interview-text">You have no applications to edit or remove at this time. Please click home and add an application to edit or remove.</h1>
  <% end %>
  <footer>
     <p id="footer-text">Jobservation 2020</p>
   </footer> 
</body>
</html>