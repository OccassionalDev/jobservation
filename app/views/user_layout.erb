<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jobservation</title>
    <link rel="stylesheet" type="text/css" href="stylesheets/user_layout.css">
    <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700&display=swap" rel="stylesheet">
</head>
<body>
    
    <nav>
        <ul id="nav-bar">
          <% if is_logged_in?(session) %>
            <li>
              <a href="/organizer" class="nav-bar-link">Home</a>
            </li>

            
              <li>
                  <a href="/logout" id="logout-link" class="nav-bar-link">Logout</a>
              </li>

              <% else %>
              <li>
                <a href="/" class="nav-bar-link">Home</a>
              </li>
          <% end %>
        </ul>
      </nav>

      <% flash.keys.each do |type| %>
        <div data-alert class="flash <%= type %> alert">
          <%= flash[type] %>
        </div>
      <% end %>

      <%= yield %>

      <footer>
        <p id="footer-text">Jobservation 2020</p>
      </footer> 
</body>
</html>